export {}; 

import React from 'react';

const Button: React.FC = () => {
  return (
    <button className="btn" type="button">
      Submit
    </button>
  );
};

export default Button;
 